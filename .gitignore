# -------------------------------------------------
# Frontend artefacts excluded from version control.
# 프런트엔드 산출물을 버전 관리에서 제외합니다.
# -------------------------------------------------
node_modules/
pnpm-lock.yaml
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
.vite/
.cache/
.coverage/
.DS_Store

# Exclude build outputs generated by the frontend apps.
# 프런트엔드 앱이 생성하는 빌드 결과물을 제외합니다.
front/apps/**/dist/
front/apps/**/build/
front/apps/**/.rollup.cache/
front/apps/**/.turbo/

# Storybook and similar static exports remain local only.
# 스토리북 등 정적 산출물은 로컬에만 유지합니다.
storybook-static/

# -------------------------------------------------
# Python backend caches and virtual environments.
# 파이썬 백엔드 캐시와 가상 환경을 제외합니다.
# -------------------------------------------------
__pycache__/
*.pyc
*.pyo
*.pyd
*.pytest_cache/
*.coverage
.coverage.*
htmlcov/
.mypy_cache/
.venv/
venv/
.env
.env.*
.env.local
*.sqlite3

# Python build artefacts that are regenerated per release.
# 릴리스마다 다시 생성되는 파이썬 빌드 산출물입니다.
build/
dist/
*.egg-info/
*.egg

# -------------------------------------------------
# Docker and container build leftovers.
# 도커 및 컨테이너 빌드 잔여물을 제외합니다.
# -------------------------------------------------
*.tar

# -------------------------------------------------
# Editor and operating system noise.
# 에디터 및 운영체제에서 생기는 노이즈 파일입니다.
# -------------------------------------------------
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

*.swp
*.swo
Thumbs.db
.DS_Store

# -------------------------------------------------
# Local runtime secrets and overrides.
# 로컬 실행 시 사용하는 비밀 정보와 오버라이드입니다.
# -------------------------------------------------
.env
*.env
.env.*.local
docker-compose.override.yml

# -------------------------------------------------
# Bundles produced for CDN distribution.
# CDN 배포를 위해 생성된 번들입니다.
# -------------------------------------------------
front/apps/collector-js/dist/
front/apps/dashboard/dist/
